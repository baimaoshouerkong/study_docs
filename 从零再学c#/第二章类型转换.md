```
float a = 1.0f;
double b = 1.0d;
// f = d;错误
f = (float)d; // 正确，double类型可以显式转换为float类型
d = f; // 正确，double类型可以隐式转换为float类型
int i = 1;
short s = 2;
// i = s;错误
i = (int)s; // 正确，short类型可以显式转换为int类型
s = (short)i; // 正确，int类型可以显式转换为short类型
```